-- Run this in MySQL to create the database (adjust user/password as needed):
-- mysql -u root -p < setup_db.sql

CREATE DATABASE IF NOT EXISTS code_assessment_db
  CHARACTER SET utf8mb4
  COLLATE utf8mb4_unicode_ci;
